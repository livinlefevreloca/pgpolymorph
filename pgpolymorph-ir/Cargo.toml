[package]
name = "pgpolymorph-ir"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Intermediate representation and COPY binary codec for PostgreSQL FORMAT binary data"

[features]
serde = ["dep:serde"]

[dependencies]
thiserror = "2"
serde = { version = "1", optional = true, features = ["derive"] }

[dev-dependencies]
